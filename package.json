{
  "name": "remote-agent-tui",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix tui-web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix tui-web",
    "build": "npm run build --prefix server && npm run build --prefix tui-web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}