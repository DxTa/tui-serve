# Set AUTH_TOKEN to require authentication, or leave empty for no auth (trusted networks only)
AUTH_TOKEN=
# Production default: 5555. Dev script (npm run dev) defaults to 3100.
PORT=5555
NODE_ENV=development