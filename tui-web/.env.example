# Dev server port (Vite auto-increments if port is busy)
VITE_DEV_PORT=5173

# Backend API port that the dev server proxies to
VITE_API_PORT=3100

# Backend API host that the dev server proxies to
VITE_API_HOST=127.0.0.1